# Local credentials
.env*
!.env.example

# Local agent planning and handoff state
.hermes/

# Generated browser E2E artifacts
/harness/evidence/

# E2E contract written by a run missing SM_HARNESS_STATIC_ROOT: the code
# default static root is the tracked harness/sm-harness-web-ui/static/, so
# only the generated contract file itself is ignored there.
/harness/sm-harness-web-ui/static/e2e-contract.json
