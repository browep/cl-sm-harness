# Playwright install artifacts. The lockfile is tracked (issue #137): the
# image build's `npm ci` hard-fails without one, so ignoring it made the
# web-ui image unbuildable from a fresh clone.
node_modules/
